<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hospital.mapper.AddMapper">
    <select id="addList" resultType="com.hospital.entity.Additional">
       SELECT `id`,`single`,`number`,`price` FROM `additional`
    </select>
</mapper>